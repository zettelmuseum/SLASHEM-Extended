name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Action 7z
        uses: DuckSoft/create-7z-action@v1.0
        with:
          pathSource: ./
          pathTarget: build.7z
      - name: upload 7z
        uses: actions/upload-artifact@v1
        with:
          name: my-artifact
          path: build.7z
          
          
          
          
          
          
          
          :  
